<div class="tweet-form">
  <%= form_with(model: @tweet, local: true) do |form| %>
    <h3>投稿する</h3>
    <div class="t-form">
      <%= form.text_area :text, class: "t-text",placeholder: "text" , rows: "10" %>
      <%= form.submit "SEND" ,class:"btn"%>
    </div>
  <% end %>
</div>